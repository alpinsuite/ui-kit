# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code
.vscode/

# Flutter / Dart / pub
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
coverage/

# A library must not pin its dependents' resolution, so the lockfile is not
# committed. The example app is part of this repository rather than a released
# artifact, so its lockfile is not committed either.
pubspec.lock
example/pubspec.lock

# Symbolication and obfuscation
app.*.symbols
app.*.map.json
